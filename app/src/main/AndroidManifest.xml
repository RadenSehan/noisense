<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.noisense">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:networkSecurityConfig="@xml/network_security_config"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_noisense"
        android:label="Noisense"
        android:roundIcon="@mipmap/ic_noisense_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AudioRecorderApp"
        tools:targetApi="31">
        <activity
            android:name="com.example.noisense.MainActivity"
            android:exported="true"
            android:label="Noisense"
            android:theme="@style/Theme.AudioRecorderApp">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>